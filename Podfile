# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FancyWeather' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for FancyWeather
  pod 'RxSwift', '6.5.0'
  pod 'RxCocoa', '6.5.0'
  target 'FancyWeatherTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'FancyWeatherUITests' do
    # Pods for testing
  end

end
